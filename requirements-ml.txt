# ==================================================
# Dependências de Machine Learning (BERT Training)
# Rodar APENAS localmente — NÃO incluir na imagem de deploy
# ==================================================
# pip install -r requirements-ml.txt

torch>=2.0.0
transformers>=4.30.0
scikit-learn>=1.3.0
